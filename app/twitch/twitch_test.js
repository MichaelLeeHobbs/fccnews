'use strict';

describe('myApp.twitch module', function() {

  beforeEach(module('myApp.twitch'));

  describe('twitch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var twitchCtrl = $controller('TwitchCtrl');
      expect(twitchCtrl).toBeDefined();
    }));

  });
});